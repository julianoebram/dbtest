{
  "name": "json-server-heroku",
  "version": "1.0.0",
  "description": "JSON Server deployment on Heroku",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "engines": {
    "node": "14.x"
  }
}
