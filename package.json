{
  "name": "meu-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3001"  
  },
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "engines": {
    "node": "14.x"
  }
}
