{
  "name": "meu-app",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "dev": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "engines": {
    "node": "14.x"
  }
}
